<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="hr_timesheet_sheet_form" model="ir.ui.view">
        <field name="name">hr.timesheet.sheet.form</field>
        <field name="model">hr_timesheet.sheet</field>
        <field name="inherit_id" ref="hr_timesheet_sheet.hr_timesheet_sheet_form" />
        <field name="priority" eval="100" />
        <field name="arch" type="xml">
            <!-- TODO: This is set readonly until such a time that we can
                 display hours_worked in the summary view instead of
                 unit_amount. -->
            <xpath expr="//field[@name='line_ids']" position="attributes">
                <attribute name="readonly">1</attribute>
            </xpath>

            <xpath
                expr="//field[@name='timesheet_ids']/tree/field[@name='unit_amount']"
                position="before"
            >
                <field
                    name="hours_worked"
                    widget="float_time"
                    string="Hours Worked"
                    sum="Hours Worked"
                />
            </xpath>

            <xpath
                expr="//field[@name='timesheet_ids']/form//field[@name='unit_amount']"
                position="before"
            >
                <field name="hours_worked" widget="float_time" string="Hours Worked" />
            </xpath>
        </field>
    </record>

    <menuitem
        name="Overtime"
        id="menu_hr_timesheet_overtime"
        sequence="99"
        parent="hr_timesheet.timesheet_menu_root"
    />

    <menuitem
        name="Overtime Rate"
        id="menu_hr_timesheet_overtime_rate"
        sequence="100"
        parent="menu_hr_timesheet_overtime"
        groups="hr.group_hr_manager"
        action="action_resource_overtime_form"
    />

</odoo>
